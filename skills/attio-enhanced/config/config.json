{
  "name": "attio-enhanced",
  "version": "1.0.0",
  "description": "Enhanced Attio CRM API skill with batch operations",
  "author": "captmarbles",
  "homepage": "https://github.com/capt-marbles/attio-enhanced",
  "license": "MIT",
  "required_env_vars": [
    {
      "name": "ATTIO_API_KEY",
      "description": "Your Attio API key from https://app.attio.com/settings/api",
      "required": true
    },
    {
      "name": "ATTIO_WORKSPACE_ID",
      "description": "Your Attio workspace ID (found in Attio URL: app.attio.com/[workspace-id]/...)",
      "required": true
    }
  ],
  "install": {
    "type": "pip",
    "requirements": "requirements.txt",
    "note": "No file creation - set environment variables before use"
  },
  "runtime": {
    "type": "python",
    "entrypoint": "lib/attio_enhanced.py",
    "note": "Requires ATTIO_API_KEY and ATTIO_WORKSPACE_ID environment variables"
  }
}
