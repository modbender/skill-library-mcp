# Changelog

## v1.0.0 — 2026-02-21

### Added
- Initial release
- Browser-based model extraction from OpenRouter app leaderboard
- API verification of all model IDs before adding
- Atomic config writes with timestamped backups
- Auto-detection of agent directory
- Externalized alias map (`references/aliases.json`)
- Support for `--dry-run`, `--json`, and stdin input
- Environment variable overrides (`OPENCLAW_DIR`, `OPENCLAW_AGENT_DIR`)
